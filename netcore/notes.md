Tasks:

1. "add a way to call the calculation with different inputs, preferrably over HTTP. " -> ASP.NET Core Web API, exposing e.g. POST /tax with a vehicle type and a list of timestamps (A new project targeting net10.0, it's long out of support, so I'd retarget it to net10.0)
2. Create the skeleton , structure only



the provided code treats tractors as exempt; the specification doesn't list them. I followed the specification / the code, because X.
Tractor — did I make the right call? I followed the spec.
Buss is missinf from TollFreeVehicles, I would add it

